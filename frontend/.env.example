# Copy to `.env` and adjust if the API host changes (no trailing slash).
VITE_API_BASE_URL=https://intex-backend-fvgedfcwcxf8cnc9.australiaeast-01.azurewebsites.net

# Where Vite proxies `/api` during local dev (`npm run dev`). Default: http://localhost:5180
# (run the .NET API with the "http" launch profile, or set this to your deployed API URL).
# VITE_DEV_PROXY_TARGET=https://intex-backend-fvgedfcwcxf8cnc9.australiaeast-01.azurewebsites.net
